import * as Actions from '../Actions/resumeBuilder.actions'
const initialState={
    resumeInput:'',
}
const ResumeBuilderReducer = (state=initialState,action)=>{
    switch(action.type){
        default:{
            return state
        }
    }
}
export default ResumeBuilderReducer;